[project]
name = "quarto-py-hello"
version = "0.1.0"
description = "Render a sine plot and a calculation."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.10.8",
]
